# CI/CD Pipeline Security Hardening Lab

A DevSecOps portfolio project that demonstrates how to build a secure CI/CD pipeline using GitHub Actions, Gitleaks, Trivy, Docker, protected branches, and gated deployments.

## Project Overview

This lab shows how to:

- run automated tests on pull requests
- detect leaked secrets before merge
- scan the codebase for high and critical issues
- protect the `main` branch with pull request rules
- require deployment approval through a production environment
- build and publish a Docker image to GitHub Container Registry
- generate build provenance attestation for stronger supply chain integrity

## Stack

- GitHub
- GitHub Actions
- Node.js
- Express
- Docker
- Gitleaks
- Trivy

## Repository Structure

```text
secure-cicd-lab/
├── .github/
│   └── workflows/
│       ├── ci.yml
│       └── release.yml
├── __tests__/
│   └── app.test.js
├── .dockerignore
├── .gitignore
├── .pre-commit-config.yaml
├── Dockerfile
├── README.md
├── app.js
├── package.json
````

## Local Setup

Clone the repository and install dependencies:

```bash
git clone https://github.com/YOUR_USERNAME/secure-cicd-lab.git
cd secure-cicd-lab
npm install
```

Run the tests:

```bash
npm test
```

Start the app:

```bash
npm start
```

Open:

```text
http://localhost:3000
```

You should see:

```json
{
  "app": "secure-cicd-lab",
  "status": "ok"
}
```

## Security Controls Included

* pull request based delivery
* branch protection on `main`
* required status checks
* Gitleaks secret scanning
* Trivy filesystem scanning
* Trivy image scanning
* production environment approval gate
* least-privilege GitHub Actions permissions
* optional build provenance attestation

## GitHub Configuration Required

Before the workflows will behave correctly, configure the repository like this:

### 1. Protect the `main` branch

Go to:

* **Settings**
* **Branches**
* add a branch protection rule for `main`

Enable:

* require a pull request before merging
* require approvals
* require status checks to pass before merging

### 2. Create a `production` environment

Go to:

* **Settings**
* **Environments**
* **New environment**

Create:

```text
production
```

Enable:

* required reviewers
* deployment branch restriction to `main`

### 3. Set GitHub Actions permissions

Go to:

* **Settings**
* **Actions**
* **General**

Set default workflow permission to:

```text
Read repository contents
```

## Test Cases for Portfolio Proof

### Secret leak test

Create a temporary file:

```text
AWS_SECRET_ACCESS_KEY=abcd1234abcd1234abcd1234abcd1234abcd1234
```

Push it to a test branch and open a pull request.

Expected result:

* Gitleaks fails
* merge is blocked

### Vulnerability test

Introduce an intentionally risky container change, then open a pull request.

Expected result:

* Trivy flags the issue
* merge is blocked

## Portfolio Value

This project shows practical ability in:

* CI/CD security
* GitHub Actions
* container security
* secret detection
* release governance
* supply chain awareness
* security documentation

## Suggested Screenshots for GitHub or LinkedIn

* repository overview
* workflow run success
* Gitleaks failure example
* Trivy failure example
* protected branch configuration
* production environment approval
* published image in GHCR
* attestation result

## `package.json`

```json
{
  "name": "secure-cicd-lab",
  "version": "1.0.0",
  "description": "A DevSecOps portfolio project demonstrating CI/CD pipeline hardening with GitHub Actions, Gitleaks, Trivy, protected branches, and gated deployments.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest --runInBand"
  },
  "keywords": [
    "devsecops",
    "github-actions",
    "cicd",
    "gitleaks",
    "trivy",
    "docker",
    "security"
  ],
  "author": "VERIQTA",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.1.0"
  }
}
```

---

## `app.js`

```javascript
const express = require("express");

const app = express();

app.get("/", (req, res) => {
  res.status(200).json({
    app: "secure-cicd-lab",
    status: "ok"
  });
});

if (require.main === module) {
  const port = process.env.PORT || 3000;
  app.listen(port, () => {
    console.log(`App listening on port ${port}`);
  });
}

module.exports = app;
```

---

## `__tests__/app.test.js`

```javascript
const request = require("supertest");
const app = require("../app");

describe("GET /", () => {
  it("should return app status", async () => {
    const res = await request(app).get("/");

    expect(res.statusCode).toBe(200);
    expect(res.body).toEqual({
      app: "secure-cicd-lab",
      status: "ok"
    });
  });
});
```

---

## `.gitignore`

```gitignore
node_modules/
.env
coverage/
dist/
.cache/
.DS_Store
```

---

## `.dockerignore`

```dockerignore
node_modules
npm-debug.log
.git
.github
coverage
.cache
```

---

## `.pre-commit-config.yaml`

```yaml
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.24.2
    hooks:
      - id: gitleaks
```

---

## `Dockerfile`

```dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY app.js ./

EXPOSE 3000

USER node

CMD ["node", "app.js"]
```

---

## `.github/workflows/ci.yml`

```yaml
name: CI Security Pipeline

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-and-scan:
    name: Test and Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: Run Gitleaks secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@0.33.1
        with:
          scan-type: fs
          scan-ref: .
          format: table
          exit-code: "1"
          ignore-unfixed: true
          severity: CRITICAL,HIGH
```

---

## `.github/workflows/release.yml`

```yaml
name: Release Secure Image

on:
  workflow_run:
    workflows:
      - CI Security Pipeline
    types:
      - completed

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  release:
    name: Build, Scan, and Publish Image
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout tested commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Normalize image name
        run: |
          IMAGE_ID=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          IMAGE_ID=$(echo "$IMAGE_ID" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_ID=$IMAGE_ID" >> $GITHUB_ENV

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build Docker image
        run: |
          docker build -t $IMAGE_ID:${{ github.event.workflow_run.head_sha }} .
          docker tag $IMAGE_ID:${{ github.event.workflow_run.head_sha }} $IMAGE_ID:latest

      - name: Run Trivy image scan
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: ${{ env.IMAGE_ID }}:${{ github.event.workflow_run.head_sha }}
          format: table
          exit-code: "1"
          ignore-unfixed: true
          severity: CRITICAL,HIGH

      - name: Push Docker image
        run: |
          docker push $IMAGE_ID:${{ github.event.workflow_run.head_sha }}
          docker push $IMAGE_ID:latest

      - name: Get pushed image digest
        id: image_digest
        run: |
          DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' $IMAGE_ID:${{ github.event.workflow_run.head_sha }} | awk -F'@' '{print $2}')
          echo "digest=$DIGEST" >> $GITHUB_OUTPUT

      - name: Generate build provenance attestation
        uses: actions/attest-build-provenance@v3
        with:
          subject-name: ${{ env.IMAGE_ID }}
          subject-digest: ${{ steps.image_digest.outputs.digest }}
          push-to-registry: true
```

---

## Commands to run after pasting the files

```bash
npm install
npm test
git add .
git commit -m "Initial DevSecOps CI/CD hardening lab"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/secure-cicd-lab.git
git push -u origin main
````



Use `YOUR_USERNAME` as your real GitHub username.
